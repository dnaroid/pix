{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default capability for the main window. Custom commands (#[tauri::command]) need no explicit permission; only plugin commands do. Includes dialog (folder picker) and shell plugins.",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "core:window:default",
    "core:webview:default",
    "core:app:default",
    "core:event:default",
    "dialog:default",
    "shell:default"
  ]
}
