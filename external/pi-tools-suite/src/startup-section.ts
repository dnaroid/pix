type StartupSection = {
	id: string;
	title: string;
	body: string;
};

export async function publishStartupModuleList(moduleNames: string[]): Promise<void> {
	void moduleNames;
}

export function publishStartupSection(section: StartupSection): void {
	void section;
}
