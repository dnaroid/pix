# Fixture instructions

This project is a benchmark fixture. Do not install dependencies or call external
services. When investigating, cite exact file paths and symbols. The obvious
business-language files include intentional decoys; verify the mutation site by
following data flow before answering.
