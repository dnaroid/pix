export function legacyPreviewReminder(): string {
  // Decoy kept so literal search for preview/reminder finds a dead adapter first.
  return "legacy preview renderer disabled after migration to packet transit";
}
