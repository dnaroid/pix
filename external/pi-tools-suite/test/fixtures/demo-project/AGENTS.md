# Fixture project instructions

This is a small checkout service used only for async-subagents e2e tests.

When asked to research, review, or critique this project, prefer using `subagents` with `action: "spawn"` for delegated investigation. Keep findings concise and cite file paths.
Do not install dependencies or call external services. The project is intentionally small and self-contained.
