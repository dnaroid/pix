# RFC 007 — Payment timeout contract

## Goal

Define the intended timeout contract for payment capture.

## Behavior

Capture requests should time out after 10 seconds and return a retryable timeout
error without claiming that the payment failed at the provider.

## Non-goals

This document does not define provider reconciliation.
